package org.example.hw.quicksort;

public class QuickSort {

    public static void main(String[] args) {

    }

    public Integer[] divisionArrays(Integer[] insertArray) {
        //опорный элемент определим как: (сначала если длина меньше 2, то возвращаем массив, как в слиянием)
        // определим середину: если insertArray.length%2 !=0 (нечетное кол-во) => insertArray.length%2/2+1, иначе
        // ((insertArray.length%2/2+1)+(insertArray.length%2/2-1))/2 - но так мы получим среднее между
        // соседними элементами значение, но этого значения же нет в массиве, это просто среднее, подумать.

    }

    public Integer[] sortArrays(Integer[] leftArray, Integer[] rightArray){

    }

}
